# Coder (docs) — Artifact

**Task:** 2026-09-23_fechamento-adr005-e-travas
**Modelo:** sonnet
**Escopo:** consistência de documentação (README, ONBOARDING, DESENVOLVIMENTO-COM-IA, EVIDENCIAS, ISO-MAPPING, ADR-001/002/005, PROPOSTA-PIPELINE-FLUXO, praticas/00, multi-agents/ARCHITECTURE, multi-agents/HANDOFF-PROTOCOL, multi-agents/agents/*)

## files_changed

- `README.md` — bump de data; tree com `scripts/check-pii.sh`, `docs/COMPETENCIA.md`, `docs/RUNBOOK-INCIDENTE-IA.md`, `docs/PROPOSTA-PIPELINE-FLUXO.md`, `docs/patches/`; menção a MCP (praticas/11); alinhamento da coluna de comentário em `templates/`; lista de cópia (bash + PowerShell) trocada para o canônico (`.claude/`, `multi-agents/`, `praticas/`, `scripts/`, `tasks/_TEMPLATE/`, `docs/`, README/DESENVOLVIMENTO-COM-IA/ONBOARDING/GOVERNANCE), com nota de que `PENDENCIAS`/`patches` nascem vazios e `COMPETENCIA.md` fica no playbook.
- `ONBOARDING.md` — bump de data; caveat de transição de branch protection (0 aprovações → 1 + `enforce_admins`) alinhado ao patch pendente e a `docs/PENDENCIAS-TECH-LEAD.md`, sem duplicar o texto já existente; lista de cópia para adoção alinhada ao canônico com a mesma nota de PENDENCIAS/patches/COMPETENCIA; ponto de entrada trocado de "orchestrator" para `/task` (ADR-005), com o fluxo manual documentado como fallback (Dynamic workflows em `/config`, plano pago) e registro no run-log; ajuste da linha "erros comuns".
- `DESENVOLVIMENTO-COM-IA.md` — bump de data; reformulação do texto sobre o Reviewer (sem ferramenta `Edit`; grava só o próprio artifact via `Write`; lê via `Read`/`Glob`/`Grep`/`Bash`) a partir do frontmatter real de `.claude/agents/reviewer.md`; reword do deny de `git clean -f*` creditando o hook `block-dangerous-git.mjs` pelas variantes além de `-fd` (único padrão literal no `settings.json`); nota de que o ponto de entrada sob ADR-005 é `/task`, com o Orchestrator como fallback.
- `praticas/00-stack-e-defaults-gbpa.md` — bump de data; caveat de transição de branch protection na linha da tabela, apontando para `docs/PENDENCIAS-TECH-LEAD.md`; retenção "5 anos" → "até 5 anos (desidentificado)".
- `docs/EVIDENCIAS-E-METRICAS.md` — bump de data; linha dos ADRs em §4 passa a "ADR-001 e 005 trimestral; 002 a 004 semestral"; novas linhas para `multi-agents/templates/` (anual), `tasks/_TEMPLATE/` (anual/a cada mudança de handoff) e `scripts/check-pii.sh` (trimestral, junto com praticas/06); notas de que `.claude/agents/` e `.claude/workflows/` entram na zona protegida via patch pendente (`.claude/hooks/` e `.claude/settings.json` já protegidos hoje; `.claude/skills/` segue writável); comando de busca de vencidos passa a casar também `**Revisão:**` (ADRs).
- `docs/ISO-MAPPING.md` — bump de data; A.8.4 explicita que o deny hoje cobre `settings.json`/`hooks/` e que `workflows/`/`agents/` entram pelo patch pendente; A.8.12 troca a alegação de `permissions.deny` de segredos (inexistente) pelos controles reais (gitleaks + `scripts/check-pii.sh`) e cita o deny de `.env*` como pendente; A.6.2.4 credita o gate por código do `gbpa-task.js` como primeira linha (hook como segunda) e nota o piloto pendente. Nenhum status alterado.
- `docs/ADR-001-modelos-por-agente.md` — `**Última revisão:** 2026-09-23`; "plano Claude Max" → "plano de subscrição (classe a confirmar — `docs/PENDENCIAS-TECH-LEAD.md` item 1)"; correção da frase sobre nome de arquivo (base name fixo, só o `name:` do frontmatter muda) + nota sobre `agentType` hard-codeados em `gbpa-task.js`.
- `docs/ADR-002-agente-security-sre.md` — `**Última revisão:** 2026-09-23`; marcador `[Superado — ADR-003]` na frase sobre os agentes 09–11 em backlog.
- `docs/ADR-005-orquestracao-nativa-do-fluxo.md` — `**Última revisão:** 2026-09-23`; Status ganha "— vigência plena depende do patch do GOVERNANCE (...) e do piloto"; nova consequência de trava sobre `.claude/workflows/`/`.claude/agents/` entrarem na zona protegida e sobre os `agentType` hard-codeados; referência genérica a `docs/patches/` trocada para `docs/patches/GOVERNANCE.proposto.md`.
- `docs/PROPOSTA-PIPELINE-FLUXO.md` — campo `**Revisão:**` adicionado; data de "Última revisão" corrigida de 2026-09-01 para 2026-09-15 (coerente com a data do ADR-005 que ela originou); nota de arquivamento quando o ADR estiver em vigor plena e o piloto tiver rodado.
- `multi-agents/ARCHITECTURE.md` — bump de data; as duas referências a `03-TECHNOLOGY/Stacks-Arquiteturas-Estado-Da-Arte-2026.md` marcadas como "(vault externo, fora deste repo)".
- `multi-agents/HANDOFF-PROTOCOL.md` — bump de data (já refletia o ADR-005 em prosa; só a data estava atrasada).
- `multi-agents/agents/00-orchestrator.md` … `12-security-sre.md` (13 arquivos) — adicionado o cabeçalho `> **Dono:** Tech Lead · **Revisão:** a cada mudança de escopo ou de modelo do agente (ADR-001) · **Última revisão:** AAAA-MM-DD` logo após o H1, com a data de `git log -1 --format=%cs -- <arquivo>` de cada um (2026-09-15 para 00 e 01; 2026-08-31 para 08, 11, 12; 2026-08-04 para os demais).

## Verificação

- **Links relativos:** script Python percorreu todo `.md` do repo, resolveu cada link não-HTTP contra o arquivo de origem — **0 links quebrados**.
- **Contagem ISO-MAPPING:** recontagem mecânica das colunas de status nas duas tabelas (27001 e 42001) após as edições — 27001: **25 OK · 9 PARCIAL · 0 LACUNA · 6 ORG** (24 diretos + A.8.30 "OK por analogia"); 42001: **22 OK · 8 PARCIAL · 0 LACUNA · 3 ORG**. Ambas batem com o resumo declarado no documento; nenhum status foi alterado, só o texto de evidência.
- **`GOVERNANCE-fluxo-por-script`:** `grep -rn "GOVERNANCE-fluxo-por-script" --include=*.md .` só retorna `docs/PENDENCIAS-TECH-LEAD.md` e `docs/patches/README.md` — nenhum dos arquivos que possuo cita mais o nome antigo do patch.

## Não fiz / fora do meu escopo

- Não toquei `docs/patches/*`, `docs/PENDENCIAS-TECH-LEAD.md`, `.claude/*`, `scripts/check-pii.sh`, `praticas/06-devsecops.md` — são do outro Coder.
- Não apliquei nada em `GOVERNANCE.md` (zona negada por hook) — as referências que fiz a §2.6/§5.4/§6.2 apontam para o patch pendente, sem prescrever o texto final, que é responsabilidade de `docs/patches/GOVERNANCE.proposto.md`.
- `praticas/00-stack-e-defaults-gbpa.md` linha de retenção "5 anos": segui a instrução literal do brief (troca por "até 5 anos (desidentificado)"), mas registro a ressalva: essa célula descreve a política de retenção da Anthropic para plano de consumidor (treino ligado), não um controle de desidentificação operado pela GBPA — o Tech Lead pode querer revisar a redação exata quando confirmar a classe do plano (item 1 de `docs/PENDENCIAS-TECH-LEAD.md`).
- Não confirmei anchors de Markdown (`#secao`) nos links que adicionei (ex.: `README.md#adotando-em-um-repositório`) — o verificador usado resolve arquivo, não âncora; âncora seguindo a convenção padrão do GitHub (minúsculas, espaços→hífen) deve resolver, mas não testei renderização.
